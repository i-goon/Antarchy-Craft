#List of slots to create or modify.
#See documentation for syntax: https://docs.illusivesoulworks.com/curios/configuration#slot-configuration
#
#Existing Antarchy Craft worlds stored charm as 7 wide (6 base plus a Relics item bonus) while the
#current mod set builds the base at 6, and Curios restores the saved width into a base-sized handler
#and refuses the player. Adding a slot raises the base back to 7 to match those saves.
slots = [
  "id=charm;size=1;operation=ADD",
]
