#List of slots to create or modify.
#See documentation for syntax: https://docs.illusivesoulworks.com/curios/configuration#slot-configuration
#
#Left empty on purpose. An earlier attempt pinned every slot type here to stop a slot size from
#drifting, but Curios 9.5.1 cannot migrate a size change: it builds the new handler at one size and
#restores the saved items at the other, which throws "Slot N not in valid range" and makes the player
#data unloadable. Existing worlds must keep the layout they were saved with.
slots = []
